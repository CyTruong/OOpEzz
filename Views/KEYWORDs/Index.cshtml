@model IEnumerable<OE.Models.KEYWORD>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EN)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VI)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RELATE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ORDER)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.STATUS)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EN)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VI)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RELATE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ORDER)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.STATUS)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PK_SEQ }) |
            @Html.ActionLink("Details", "Details", new { id=item.PK_SEQ }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PK_SEQ })
        </td>
    </tr>
}

</table>
